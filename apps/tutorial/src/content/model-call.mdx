<LessonOverview
  goal="CLI 能通过同一 Model 接口运行确定性的离线 Fake Model，并可选择连接真实模型。"
  task="先离线验证“只回复：模型已连接”，再选择是否用真实 API 重跑。"
  prerequisites="已经完成第 00 章；真实 API Key 仅用于可选的在线验证。"
  files={["package.json", ".env", ".gitignore", "src/model.ts", "src/fake-model.ts", "src/index.ts", "tests/model.test.ts"]}
/>

<Step id="install-sdk" number={1} title="安装 OpenAI SDK">

```shell
pnpm add openai@7.15.0
```

SDK 只存在于模型适配器中。Harness 不会直接依赖它。

</Step>

<Step id="configure-env" number={2} title="配置本地环境">

真实调用按 token 计费，具体价格和可用模型会随账号与时间变化；运行前查看 [OpenAI API Pricing](https://openai.com/api/pricing/)，设置项目预算与用量告警。模型能力要求是：支持文本生成、结构化输出和 function calling，并有足够的上下文窗口。本教程不要求某个固定模型 ID，学习阶段优先选择支持这些能力的低成本模型。

如果要进行在线验证，创建 `.env` 并填写账号可用的模型 ID。不要把 API Key 写进源码或提交到 Git。

```dotenv
OPENAI_API_KEY=your-local-key
OPENAI_MODEL=your-model-id
```

同时创建 `.gitignore`：

```gitignore
.env
node_modules
dist
```

</Step>

<Step id="call-model" number={3} title="完成第一次 Responses API 调用">

创建 `src/model.ts`。[官方 Responses API](https://developers.openai.com/api/reference/cli/resources/responses/methods/create) 使用 `input` 接收文本，并可通过 `output_text` 取得聚合后的文本输出。

```typescript
import OpenAI from "openai";

const client = new OpenAI();

export async function askModel(input: string): Promise<string> {
  const model = process.env.OPENAI_MODEL;
  if (!model) throw new Error("缺少 OPENAI_MODEL");

  const response = await client.responses.create({ model, input });
  return response.output_text;
}
```

在 `src/index.ts` 中调用它：

```typescript
import { askModel } from "./model.js";

const task = process.argv.slice(2).join(" ").trim();
if (!task) throw new Error("请提供任务");

console.log(await askModel(task));
```

</Step>

<Step id="extract-port" number={4} title="抽出最小 Model 接口">

把函数包装成接口，为后面的假模型测试和提供商替换留下边界。

```typescript
export interface Model {
  generate(input: string): Promise<string>;
}

export const model: Model = {
  generate: askModel,
};
```

</Step>

<Step id="run-fake-model" number={5} title="先用 Fake Model 离线验证">

创建 `src/fake-model.ts`。Fake Model 是确定性的系统边界替身，不读取 API Key、不访问网络，也不产生费用。

```typescript
import type { Model } from "./model.js";

export class FakeModel implements Model {
  constructor(private readonly replies: string[]) {}

  async generate(_input: string): Promise<string> {
    const reply = this.replies.shift();
    if (!reply) throw new Error("fake_model_has_no_reply");
    return reply;
  }
}
```

在 `tests/model.test.ts` 注入 `new FakeModel(["模型已连接"])`，断言输入经过相同 Model 接口并得到固定回复。CLI 可用 `MODEL_MODE=fake` 选择它；只有显式设置 `MODEL_MODE=openai` 时才创建 SDK Client。这样没有 Key、网络不稳定或控制成本时，也能完成本章并继续学习 Harness。

</Step>

<Checkpoint command='pnpm test -- tests/model.test.ts && MODEL_MODE=fake pnpm dev -- "只回复：模型已连接"'>

离线路径必须稳定输出“模型已连接”并通过类型检查。若你配置了 Key，再执行 `MODEL_MODE=openai pnpm dev -- "只回复：模型已连接"` 做可选在线检查；记录所用模型和 usage，不把在线调用成功作为进入下一章的硬门槛。

</Checkpoint>
