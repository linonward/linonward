<LessonOverview
  goal="任务统一进入一个有状态、有停止条件、有错误边界且可测试的 Agent Harness。"
  task="让受控 Harness 在预算内完成“解释 package.json 的作用”，并返回明确停止原因。"
  prerequisites="已经有 ModelRequest、Model 接口、可初始化的 AgentState 和经过校验的 TaskPlan。"
  files={["src/types.ts", "src/harness.ts", "src/index.ts"]}
/>

Harness 是 Agent 的确定性控制层。模型只提出下一步意图；是否继续、允许多少步、如何记录状态以及何时停止，都由 Harness 决定。

<Step id="define-responsibilities" number={1} title="划清 Harness 的职责">

先固定边界，避免把整个应用写进一个循环：

- **CLI**：解析参数、装配依赖、设置退出码；
- **Context Builder**：从状态生成本轮 `ModelRequest`；
- **Model Adapter**：调用模型并把厂商响应转成领域对象；
- **Planner**：提出初始计划、评估步骤证据，并在计划失效时给出修订草案；
- **Harness**：推进状态机、执行停止策略、调度工具；
- **Tool Executor**：校验、授权、超时、执行与序列化；
- **Reporter**：输出最终答案和验证证据。

模型不能直接访问文件系统，也不能自己把状态标记为完成。它只能返回文本或工具调用建议。

</Step>

<Step id="define-options" number={2} title="定义运行契约">

在 `src/types.ts` 加入明确的输入、输出和停止原因。上限应由调用方传入，测试时才能把它设得很小。

```typescript
import type { Model } from "./model.js";

export interface HarnessOptions {
  cwd: string;
  maxSteps: number;
  maxToolCalls: number;
  model: Model;
  signal?: AbortSignal;
  onEvent?: (event: AgentEvent) => void;
}

export type StopReason =
  | "final_answer"
  | "max_steps"
  | "max_tool_calls"
  | "cancelled"
  | "model_error"
  | "invalid_model_output";

export interface AgentResult {
  status: "completed" | "failed" | "blocked";
  answer: string;
  stopReason: StopReason;
  state: AgentState;
}

export type AgentEvent =
  | { type: "run_started"; runId: string }
  | { type: "model_started"; step: number }
  | { type: "model_completed"; step: number }
  | { type: "run_stopped"; reason: StopReason };
```

事件回调不应改变运行语义。它只负责日志、UI 流式更新或 tracing。

</Step>

<Step id="build-loop" number={3} title="实现第一个受控循环">

创建 `src/harness.ts`。本章尚未接入工具，所以模型必须给出非空最终文本；下一章会在同一循环中加入工具分支。

<AgentFlow />

```typescript
import { buildModelRequest } from "./context.js";
import { createInitialState } from "./state.js";
import type { AgentResult, AgentState, HarnessOptions, StopReason } from "./types.js";

export async function runAgent(
  task: string,
  options: HarnessOptions,
): Promise<AgentResult> {
  const state = createInitialState(task, options.cwd);
  options.onEvent?.({ type: "run_started", runId: state.runId });

  try {
    for (let step = 1; step <= options.maxSteps; step += 1) {
      if (options.signal?.aborted) return stop(state, "cancelled");

      const request = buildModelRequest({
        task: state.task,
        cwd: state.cwd,
        toolNames: [],
        sources: state.contextSources,
      });

      options.onEvent?.({ type: "model_started", step });
      const answer = (await options.model.generate(request)).trim();
      options.onEvent?.({ type: "model_completed", step });

      state.steps.push({ number: step, kind: "model", summary: "模型返回最终回答" });
      if (!answer) return stop(state, "invalid_model_output");

      state.messages.push({ role: "assistant", content: answer });
      state.status = "completed";
      return { status: "completed", answer, stopReason: "final_answer", state };
    }

    return stop(state, "max_steps");
  } catch (error) {
    const reason = options.signal?.aborted ? "cancelled" : "model_error";
    state.steps.push({
      number: state.steps.length + 1,
      kind: "model",
      summary: error instanceof Error ? error.message : String(error),
    });
    return stop(state, reason);
  }
}

function stop(state: AgentState, reason: StopReason): AgentResult {
  state.status = reason === "cancelled" ? "blocked" : "failed";
  return { status: state.status, answer: "", stopReason: reason, state };
}
```

</Step>

<Step id="handle-failures" number={4} title="统一处理失败与取消">

网络超时、限流和无效响应不能变成未处理异常。只在模型适配器中重试可恢复错误，并使用有上限的指数退避；Harness 负责把最终失败映射成稳定的 `stopReason`。

至少测试这些分支：模型抛错、返回空文本、达到步骤上限、收到 `AbortSignal`。日志可以记录错误类型、请求 ID 和重试次数，但不要记录 API Key 或未脱敏的完整上下文。

</Step>

<Step id="wire-cli" number={5} title="让 CLI 只负责装配">

更新 `src/index.ts`。入口负责信号处理和退出码，不了解循环细节。

```typescript
import { runAgent } from "./harness.js";
import { model } from "./model.js";

const task = process.argv.slice(2).join(" ").trim();
if (!task) throw new Error("请提供任务");

const controller = new AbortController();
process.once("SIGINT", () => controller.abort());

const result = await runAgent(task, {
  cwd: process.cwd(),
  maxSteps: 12,
  maxToolCalls: 24,
  model,
  signal: controller.signal,
  onEvent: (event) => console.error(JSON.stringify(event)),
});

console.log(result.answer);
process.exitCode = result.status === "completed" ? 0 : 1;
```

</Step>

<Checkpoint command='pnpm dev -- "解释 package.json 的作用"'>

程序应依次产生 `run_started → model_started → model_completed` 事件，并返回 `final_answer`。再用 Fake Model 覆盖空文本、抛错、`maxSteps: 0` 和提前取消四条路径，确认每条路径都有确定的状态与停止原因。

</Checkpoint>
